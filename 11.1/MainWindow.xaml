<Window x:Class="BookInventory.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:BookInventory"
        mc:Ignorable="d"
        Title="Book Inventory" Height="650" Width="1000"
        WindowStartupLocation="CenterScreen">
    <Grid Margin="16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <TextBlock Text="Book Inventory"
                   FontSize="28"
                   FontWeight="SemiBold"
                   Margin="0,0,0,16" />

        <Border Grid.Row="1"
                BorderBrush="#CCCCCC"
                BorderThickness="1"
                Padding="14"
                Margin="0,0,0,16">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="130" />
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="130" />
                    <ColumnDefinition Width="*" />
                </Grid.ColumnDefinitions>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="Auto" />
                </Grid.RowDefinitions>

                <TextBlock Grid.Row="0" Grid.Column="0" Text="ISBN" VerticalAlignment="Center" Margin="0,0,8,8" />
                <TextBox x:Name="IsbnTextBox" Grid.Row="0" Grid.Column="1" Margin="0,0,16,8" />

                <TextBlock Grid.Row="0" Grid.Column="2" Text="Name" VerticalAlignment="Center" Margin="0,0,8,8" />
                <TextBox x:Name="NameTextBox" Grid.Row="0" Grid.Column="3" Margin="0,0,0,8" />

                <TextBlock Grid.Row="1" Grid.Column="0" Text="Author Name" VerticalAlignment="Center" Margin="0,0,8,8" />
                <TextBox x:Name="AuthorNameTextBox" Grid.Row="1" Grid.Column="1" Margin="0,0,16,8" />

                <TextBlock Grid.Row="1" Grid.Column="2" Text="Publisher" VerticalAlignment="Center" Margin="0,0,8,8" />
                <TextBox x:Name="PublisherTextBox" Grid.Row="1" Grid.Column="3" Margin="0,0,0,8" />

                <TextBlock Grid.Row="2" Grid.Column="0" Text="Price" VerticalAlignment="Center" Margin="0,0,8,8" />
                <TextBox x:Name="PriceTextBox" Grid.Row="2" Grid.Column="1" Margin="0,0,16,8" />

                <TextBlock Grid.Row="2" Grid.Column="2" Text="Quantity" VerticalAlignment="Center" Margin="0,0,8,8" />
                <TextBox x:Name="QuantityTextBox" Grid.Row="2" Grid.Column="3" Margin="0,0,0,8" />

                <TextBlock Grid.Row="3" Grid.Column="0" Text="Description" VerticalAlignment="Top" Margin="0,4,8,8" />
                <TextBox x:Name="DescriptionTextBox"
                         Grid.Row="3"
                         Grid.Column="1"
                         Grid.ColumnSpan="3"
                         Height="80"
                         AcceptsReturn="True"
                         TextWrapping="Wrap"
                         VerticalScrollBarVisibility="Auto"
                         Margin="0,0,0,8" />

                <StackPanel Grid.Row="4"
                            Grid.Column="0"
                            Grid.ColumnSpan="4"
                            Orientation="Horizontal"
                            HorizontalAlignment="Right">
                    <Button Content="Add" Width="90" Margin="0,0,8,0" Click="AddButton_Click" />
                    <Button Content="Update" Width="90" Margin="0,0,8,0" Click="UpdateButton_Click" />
                    <Button Content="Delete" Width="90" Margin="0,0,8,0" Click="DeleteButton_Click" />
                    <Button Content="Clear" Width="90" Margin="0,0,8,0" Click="ClearButton_Click" />
                    <Button Content="Refresh" Width="90" Click="RefreshButton_Click" />
                </StackPanel>
            </Grid>
        </Border>

        <DataGrid x:Name="BooksDataGrid"
                  Grid.Row="2"
                  AutoGenerateColumns="False"
                  IsReadOnly="True"
                  SelectionMode="Single"
                  SelectionChanged="BooksDataGrid_SelectionChanged">
            <DataGrid.Columns>
                <DataGridTextColumn Header="ISBN" Binding="{Binding ISBN}" Width="140" />
                <DataGridTextColumn Header="Name" Binding="{Binding Name}" Width="*" />
                <DataGridTextColumn Header="Author Name" Binding="{Binding AuthorName}" Width="170" />
                <DataGridTextColumn Header="Description" Binding="{Binding Description}" Width="260" />
                <DataGridTextColumn Header="Publisher" Binding="{Binding Publisher}" Width="140" />
                <DataGridTextColumn Header="Price" Binding="{Binding Price, StringFormat=C}" Width="90" />
                <DataGridTextColumn Header="Quantity" Binding="{Binding QuantityInStock}" Width="90" />
            </DataGrid.Columns>
        </DataGrid>

        <TextBlock x:Name="StatusTextBlock"
                   Grid.Row="3"
                   Margin="0,12,0,0"
                   Foreground="#555555" />
    </Grid>
</Window>
